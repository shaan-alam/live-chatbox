import Header from "./Header";
import Messages from "./Messages";
import Form from "./Form";

const Home = () => {
  return (
      <div className="home">
        <Header />
        <Messages />
        <Form />
      </div>
  );
};

export default Home;
